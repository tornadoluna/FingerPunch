[pytest]

python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings

markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (may be slower)
    stats: Tests related to statistics and keystroke counting

